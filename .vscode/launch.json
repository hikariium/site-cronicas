{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve and Open in Firefox (task)",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}",
      "preLaunchTask": "serve and open",
      "console": "integratedTerminal"
    }
  ]
}
